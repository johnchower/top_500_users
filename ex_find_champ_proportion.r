# Exploration: find_champ_proportion

# Find proportion of users in a given subset who have taken a "champion only"
# action at some point.

find_champ_proportion <- function(user_subset,
                                  ,

